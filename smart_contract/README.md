# Smart Contract

Solidity smart contract for on-chain NFT minting and ownership registration.

## What is does

After a watermarked image is uploaded to IPFS, the contract mints an NFT using the returned token URI. The on-chain record ties the token to the owner's wallet address (the same address whose prefix is embedded invisibly in the image), what 

## Files

- `smartcontract/`: Solidity file of smart contract.
